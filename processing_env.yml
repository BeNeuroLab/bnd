name: bnd
channels:
  - conda-forge
  - pytorch
  - nvidia
  - defaults
dependencies:
  - pip
  - python=3.10
  - pytest
  - typer
  - pytorch
  - pytorch-cuda=11.8
  - pynwb
  - numpy
  - pandas
  - scipy
  - spikeinterface
  - pip:
      - kilosort
      - ndx_pose
      - neuroconv
      - -e ./
name: bnd-dev
channels:
  - conda-forge
  - pytorch
  - nvidia
  - defaults
dependencies:
  - pip
  - python=3.10
  - typer
  - pytorch
  - pytorch-cuda=11.8
  - pynwb
  - numpy
  - pandas
  - scipy
  - pip:
      - kilosort
      - ndx_pose
      - -e ./
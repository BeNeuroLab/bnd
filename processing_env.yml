name: bnd
channels:
  - conda-forge
  - pytorch
  - nvidia
  - defaults
dependencies:
  - pip
  - python=3.10
  - pytest
  - typer
  - pynwb
  - numpy
  - pandas
  - scipy
  - spikeinterface
  - pip:
      - ndx_pose
      - neuroconv~=0.6.0
      - -e ./